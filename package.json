{
  "name": "room",
  "version": "1.0.0",
  "description": "A light weight room chat for besties made by nodejs reactjs socket.io mongodb",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node server/index.js",
    "heroku-postbuild-backup": "concurrently \"cd client && yarn && yarn build\" \"cd server && yarn\"",
    "heroku-postbuild": "cd server && yarn"
  },
  "author": "fs-nathan",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git@github.com-fs-nathan:fs-nathan/room"
  },
  "dependencies": {
    "concurrently": "^7.2.1"
  }
}
