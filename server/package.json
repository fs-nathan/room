{
  "name": "room-server",
  "version": "1.0.0",
  "description": "Backend for Room application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-dev": "pm2 start ecosystem.config.js && pm2 monit",
    "start-production": "pm2 start ecosystem.config.js --env production"
  },
  "author": "fs-nathan",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.7.2",
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "http": "^0.0.1-security",
    "mongoose": "^6.3.5",
    "socket.io": "^4.5.1"
  },
  "devDependencies": {
    "pm2": "^5.2.0"
  }
}
